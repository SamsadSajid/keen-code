# Issue #95 — `/yolo` approval-less mode

- [x] Task 1 — `ModeYolo` + system prompt (`internal/llm/systemprompt.go`)
  - Done: added `ModeYolo`, `yoloModePrompt` (approval-less, keeps secrets line), `switch` routing in `Build()`; added `TestBuild_YoloIncludesYoloInstructions`. Files: `internal/llm/systemprompt.go`, `internal/llm/systemprompt_test.go`. Tests: `go test ./internal/llm/ -run TestBuild` all pass.
- [x] Task 2 — AppState mode handling (`internal/cli/repl/appstate/state.go`)
  - Done: `SetMode` accepts `ModeYolo` (only unknown values coerce to build); `Mode()` round-trips yolo; `EffectiveToolRegistry()` returns full registry in yolo. Files: `internal/cli/repl/appstate/state.go`, `internal/cli/repl/appstate/state_test.go`. Tests: `go test ./internal/cli/repl/appstate/ -count=1` all pass (incl. new `TestAppState_SetModeYoloRoundTrips`, `TestAppState_EffectiveToolRegistryYoloIncludesWriteEditBash`).
- [x] Task 3 — Permission bypass in the requester (`internal/cli/repl/permissions/requester.go`)
  - Done: added `yoloMode bool` + `SetYoloMode(bool)`; `RequestPermission` returns `(true, nil)` immediately when yolo set, before project/session/prompt logic; `autoApprove` and Guard paths untouched. Files: `internal/cli/repl/permissions/requester.go`, `internal/cli/repl/permissions/requester_test.go`. Tests: `go test ./internal/cli/repl/permissions/ -count=1 -v` all pass (incl. new `TestYoloMode_AllowsWithoutRequest`, `TestYoloMode_ToggleOffRestoresPrompting`).
- [x] Task 4 — Wire yolo into the REPL model (`internal/cli/repl/repl.go`, `repl_helpers.go`, `command_handlers.go:1162-1170`)
  - Done: `setMode` accepts yolo (unknown coerces to build), syncs `appState.SetMode` + `permissionRequester.SetYoloMode(mode == yolo)`; `toggleMode` cycles build→plan→yolo→build; `handleClearCommand` re-syncs yolo flag so `/clear`+`/new` preserve yolo. Files: `internal/cli/repl/repl_helpers.go`, `internal/cli/repl/command_handlers.go`, `internal/cli/repl/repl_helpers_test.go`, `internal/cli/repl/command_handlers_test.go`. Tests: focused `go test ./internal/cli/repl/ -run 'TestSetModeYolo|TestToggleModeCycles|TestHandleClearCommandPreserves|TestHandleNewCommandPreserves|TestHandleModeCommand|TestCurrentMode'` all pass (incl. new `TestSetModeYoloSyncsAppStateAndRequester`, `TestToggleModeCyclesBuildPlanYolo`, `TestHandleClearCommandPreservesYolo`, `TestHandleNewCommandPreservesYolo`). Note: full `go test ./internal/cli/repl/` has 1 expected failure — `TestHandleKeyMsg_ShiftTabTogglesMode` (handlers_test.go:593) still expects 2-state plan→build; it needs the Task-6 update to 3-state build→plan→yolo→build.
- [x] Task 5 — `/yolo` + `/mode yolo` slash commands (`internal/cli/repl/commands/commands.go`, `command_handlers.go`)
  - Done: added `Yolo = "/yolo"` const with `All`/`Suggestions` entries ("Enable approval-less yolo mode"); updated `Mode` descriptions to `(plan|build|yolo)`; added `/yolo` dispatch case via `setMode(yolo)`; extended `handleModeCommand` to accept `yolo` with `Usage: /mode plan|build|yolo` and updated bare-`/mode` hint. Files: `internal/cli/repl/commands/commands.go`, `internal/cli/repl/command_handlers.go`, `internal/cli/repl/commands/commands_test.go`, `internal/cli/repl/command_handlers_test.go`. Tests: `go test ./internal/cli/repl/commands/ -count=1` pass (incl. new `TestYoloCommandRegistered`); `go test ./internal/cli/repl/ -run 'TestHandleModeCommand|TestDispatchCommandYolo|TestGetHelpText'` pass (incl. `/mode yolo`, `/yolo` dispatch, invalid-keeps-mode, `/help` lists `/yolo`). Note: full `go test ./internal/cli/repl/` still has 1 expected failure — `TestHandleKeyMsg_ShiftTabTogglesMode` needs Task-6 3-state update.
- [x] Task 6 — `shift+tab` three-state toggle + help text (`internal/cli/repl/handlers.go:595-597`, `repl_helpers.go:32-63`, `tips.go`)
  - Done: `shift+tab` already routes via 3-state `toggleMode` (build→plan→yolo→build, Task 4 — no handlers.go change needed); updated loading tips (shift+tab cycles build→plan→yolo; `/mode build` exits plan/yolo) and tips.go (shift-tab cycles build/plan/yolo; plan tip mentions `/yolo` skips prompts). Extended `TestHandleKeyMsg_ShiftTabTogglesMode` to 3 presses with appState sync. Files: `internal/cli/repl/repl_helpers.go`, `internal/cli/repl/tips.go`, `internal/cli/repl/handlers_test.go`. Tests: `go test ./internal/cli/repl/ -count=1` all pass.
- [x] Task 7 — Red yolo input UI (`internal/cli/repl/theme/styles.go`, `repl_helpers.go:595-635`, `repl.go:869-885`)
  - Done: added `ModeYoloChipStyle` (ErrorColor bg/White fg), `YoloInputRuleStyle` (ErrorColor faint), `InputRuleYoloStyle` + `PromptYoloStyle` (ErrorColor); `renderInputArea` uses yolo rule/chip in yolo mode; `repl.go` sets `Focused.Prompt = PromptYoloStyle` in yolo. Files: `internal/cli/repl/theme/styles.go`, `internal/cli/repl/repl_helpers.go`, `internal/cli/repl/repl.go`, `internal/cli/repl/repl_test.go`. Tests: `go test ./internal/cli/repl/ -count=1` all pass (incl. new `TestInitialModel_YoloModeSetsPromptStyle`, `TestRenderInputArea_UsesErrorStyleForYoloMode`).
- [x] Task 8 — Docs + full verification
  - Done: `docs/cli-usage.md` — `/mode` row → `plan|build|yolo`, new `/yolo` table row, new `/mode [plan|build|yolo]` + `/yolo` sections (modes, 3-state `Shift+Tab`, red input indicator, `/clear`+`/new` preserve mode), `Shift+Tab` keyboard-shortcut row; `docs/permission-system.md` — lookup order now yolo → autoApprove → project allow → session → prompt, plus Yolo Mode section (per-mode Requester flag, guard `PermissionDenied` still blocks). No Go files modified. Verification: `gofmt -l` clean, `go mod tidy` ok, `go test -race ./...` all packages pass, `git diff --check` clean.
